# CLAUDE.md

Claude Code reads this file automatically. The rules for every coding agent in this
repository — Claude, Cursor and Codex alike — live in `AGENTS.md`, so there is one set
to keep current. Change the rules there, not here.

Start each session by reading `BACKLOG.md` for the open work and the newest entries at
the top of `AI_DEVELOPMENT_LOG.md`.

@AGENTS.md
