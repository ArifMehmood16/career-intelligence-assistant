# CLAUDE.md — frontend

Claude Code loads this file when working under `frontend/`. The frontend rules live in
`frontend/AGENTS.md` and the repository rules in the root `AGENTS.md`; change them
there, not here.

@AGENTS.md
